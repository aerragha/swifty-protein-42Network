import { StyleSheet } from "react-native";

export default StyleSheet.create({
    test: {
        flex: 1,
        // width: '100%',
        // height: '100%',
        backgroundColor: 'white',
    }
});
